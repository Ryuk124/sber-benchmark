# ⚡ Чек-лист: Быстрый старт после qwen-api ошибки

## 🎯 Что произошло?

Пакет `qwen-api` не существует в PyPI. **Это нормально!** Используем лучшую альтернативу.

---

## ✅ Чек-лист: Шаг за шагом

### 1️⃣ Очистить старые попытки установки
```bash
pip uninstall qwen-api  # если вы установили эту ошибку
pip uninstall qwen      # проверить разные варианты
```

### 2️⃣ Установить правильный пакет (OpenAI)
```bash
pip install openai==1.3.0
```

✅ **Должно быть**: 
```
Successfully installed openai-1.3.0
```

### 3️⃣ Проверить что установилось
```bash
python -c "import openai; print(openai.__version__)"
```

✅ **Должно вывести**: `1.3.0`

### 4️⃣ Обновить .env файл
```bash
cp backend/.env.example backend/.env
```

Отредактировать `backend/.env`:
```
OPENAI_API_KEY=sk-proj-xxxxx  # ← Получить с https://platform.openai.com/api-keys
```

### 5️⃣ Убедиться что requirements.txt обновлен
```bash
cat backend/requirements.txt | grep openai
```

✅ **Должно быть**: `openai==1.3.0`

### 6️⃣ Перезагрузить Django
```bash
cd backend
python manage.py runserver
```

✅ **Должно быть**: `Starting development server at http://127.0.0.1:8000/`

### 7️⃣ Протестировать API анализа
```bash
# Новый терминал
curl http://localhost:8000/api/ai/analysis/
```

✅ **Должно вернуть**: JSON с анализами

### 8️⃣ Перезагрузить React
```bash
npm run dev
```

✅ **Должно быть**: `Local: http://localhost:5173`

---

## 🎬 Если вы уже запустили систему раньше

### Скорый путь (2 минуты):

```bash
# 1. Backend
cd backend
pip install openai==1.3.0
python manage.py runserver

# 2. Frontend (новый терминал)
npm run dev

# 3. Готово! ✅
```

### Если не работает:

```bash
# Полная переустановка зависимостей
pip install -r requirements.txt --force-reinstall
npm install
npm run dev
```

---

## 🚀 Варианты использования

### Вариант A: OpenAI (Рекомендуется) ⭐
```bash
# 1. Получить API ключ: https://platform.openai.com/api-keys
# 2. Добавить в .env: OPENAI_API_KEY=sk-...
# 3. Готово!

# Стоимость: ~$0.0005 за анализ
# Скорость: ⚡ Быстро (1-2 сек)
# Качество: ✨ Отличное
```

### Вариант B: Локально (Ollama)
```bash
# 1. Установить Ollama: https://ollama.ai
ollama install ollama-0.1.0

# 2. Запустить модель (в отдельном терминале):
ollama run llama2

# Стоимость: Бесплатно
# Скорость: ⏱️ Медленнее (10-30 сек)
# Качество: ✅ Хорошее
```

### Вариант C: Mock (Без ключа) 🧪
```bash
# Ничего не устанавливать!
# Система автоматически использует mock-данные

# Стоимость: Бесплатно
# Скорость: ⚡⚡⚡ Очень быстро
# Качество: 📝 Тестовые данные
```

---

## 🐛 Возможные проблемы

| Проблема | Решение |
|----------|---------|
| `ModuleNotFoundError: openai` | `pip install openai==1.3.0` |
| `OPENAI_API_KEY not found` | Добавить в `.env` или системные переменные |
| `Invalid API key` | Проверить что ключ правильный на https://platform.openai.com/api-keys |
| `Connection refused` | Система вернется к mock-анализу (нормально) |
| Анализ работает но медленно | Это OpenAI API, может быть медленнее. Или использовать Ollama |

---

## 📝 Дополнительные ресурсы

- **Подробный гайд**: `LLM_SETUP.md`
- **Troubleshooting**: `TROUBLESHOOTING.md`
- **OpenAI документация**: https://platform.openai.com/docs/

---

## ✨ Что делать дальше?

### 1. Система работает с mock-анализом? ✅
→ Переходить на следующий шаг

### 2. Хотите использовать реальный AI? 🤖
→ Выполнить Вариант A выше (OpenAI)

### 3. Хотите использовать локальную модель? 🏠
→ Выполнить Вариант B (Ollama)

### 4. Хотите интегрировать реальные парсеры? 📊
→ Смотреть `IMPLEMENTATION_COMPLETE.md`

---

**Поздравляем!** 🎉 Система готова к работе!

