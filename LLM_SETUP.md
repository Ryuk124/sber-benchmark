# 🤖 LLM Integration Guide

Система поддерживает интеграцию с разными LLM провайдерами. Выберите наиболее удобный для вас.

## 🚀 Быстрый выбор

### Вариант 1: OpenAI GPT (РЕКОМЕНДУЕТСЯ)
**Преимущества**: Лучшее качество, легко настраивается, бесплатный trial  
**Потребление**: Дешевле всего

```bash
pip install openai==1.3.0
```

**Шаг 1**: Получить API ключ на https://platform.openai.com/api-keys

**Шаг 2**: Добавить в `.env`:
```
OPENAI_API_KEY=sk-proj-your-key-here
```

**Шаг 3**: Готово! Система автоматически использует GPT-3.5-turbo

---

### Вариант 2: Anthropic Claude
**Преимущества**: Хорошее качество, конфиденциальность

```bash
pip install anthropic==0.7.0
```

**Шаг 1**: Получить API ключ на https://www.anthropic.com/

**Шаг 2**: Добавить в `.env`:
```
ANTHROPIC_API_KEY=sk-ant-your-key-here
```

**Шаг 3**: Обновить `llm_service.py` для использования Claude

---

### Вариант 3: Локальная модель (Ollama, LLaMA2)
**Преимущества**: Полная приватность, без лимитов

**Установка Ollama**:
```bash
# macOS / Linux
curl -fsSL https://ollama.ai/install.sh | sh

# Или скачать с https://ollama.ai
```

**Запустить локальную модель**:
```bash
ollama run llama2
# или другую модель: mistral, neural-chat, и т.д.
```

**Интеграция в систему**:
```python
# llm_service.py - добавить
import requests

def _analyze_with_ollama(self, text, competitor, product, criterion):
    response = requests.post(
        'http://localhost:11434/api/generate',
        json={
            'model': 'llama2',
            'prompt': f'Analyze: {text}',
            'stream': False
        }
    )
    return response.json()
```

---

## 📋 Полная документация по каждому провайдеру

### OpenAI GPT

**Установка**:
```bash
pip install openai==1.3.0
```

**Модели доступны**:
- `gpt-3.5-turbo` - быстро и дешево (рекомендуется)
- `gpt-4` - лучшее качество, дороже
- `gpt-4-turbo` - быстрый GPT-4

**Пример использования**:
```python
from openai import OpenAI

client = OpenAI(api_key="sk-...")

response = client.chat.completions.create(
    model="gpt-3.5-turbo",
    messages=[
        {"role": "system", "content": "Ты эксперт по банкам"},
        {"role": "user", "content": "Проанализируй текст"}
    ]
)

print(response.choices[0].message.content)
```

**Бесплатный trial**: $5 на 3 месяца  
**Цены**: $0.0005-$0.03 за 1K токенов

**Ссылка**: https://platform.openai.com/

---

### Anthropic Claude

**Установка**:
```bash
pip install anthropic==0.7.0
```

**Модели доступны**:
- `claude-3-opus` - лучшее качество
- `claude-3-sonnet` - хороший баланс
- `claude-3-haiku` - быстро и дешево

**Пример использования**:
```python
import anthropic

client = anthropic.Anthropic(api_key="sk-ant-...")

message = client.messages.create(
    model="claude-3-sonnet-20240229",
    max_tokens=1024,
    messages=[
        {"role": "user", "content": "Проанализируй текст"}
    ]
)

print(message.content[0].text)
```

**Пробный период**: Зависит от плана  
**Цены**: $0.003-$0.03 за 1K токенов

**Ссылка**: https://www.anthropic.com/

---

### Ollama (Локальная модель)

**Установка на macOS**:
```bash
brew install ollama
# или скачать с https://ollama.ai
```

**Запуск**:
```bash
# Запустить сервер (слушает на localhost:11434)
ollama serve

# В отдельном терминале скачать модель
ollama pull llama2
ollama pull mistral
ollama pull neural-chat
```

**Интеграция**:
```python
import requests

def analyze_with_ollama(text):
    response = requests.post(
        'http://localhost:11434/api/generate',
        json={
            'model': 'llama2',
            'prompt': text,
            'stream': False
        },
        timeout=60
    )
    return response.json()['response']
```

**Требования**: 8+ GB RAM, 20+ GB диска  
**Цена**: Бесплатно  
**Скорость**: Зависит от ПК

**Доступные модели**:
- llama2 (7B) - хорошее качество
- mistral (7B) - быстро
- neural-chat (13B) - специализированная
- и многие другие

**Ссылка**: https://ollama.ai/

---

## 🔧 Конфигурация в системе

### Текущая реализация в `llm_service.py`:

```python
def _run_llm_analysis(self, text, competitor, product, criterion):
    # 1. Пробует использовать OpenAI если доступен API ключ
    # 2. Fallback на mock анализ
    
    # Для использования другого провайдера:
    # а) Установить пакет: pip install openai/anthropic/ollama
    # б) Добавить env переменную: OPENAI_API_KEY=...
    # в) Система автоматически переключится
```

---

## 📊 Сравнение провайдеров

| Критерий | OpenAI | Claude | Ollama |
|----------|--------|--------|--------|
| Качество | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| Скорость | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| Цена | 💰💰 | 💰💰💰 | 💰 (бесплатно) |
| Приватность | ❌ | ✅✅ | ✅✅✅ |
| Простота | ✅✅✅ | ✅✅ | ⚠️ |
| Лучше для | Продакшена | Конфид. | Лабораторно |

---

## 🚀 Рекомендуемая настройка

### Для начинающих:
1. Использовать **OpenAI GPT-3.5-turbo**
2. Взять пробный $5 trial
3. Легко настраивается, лучшее качество

### Для продакшена:
1. **OpenAI GPT-3.5-turbo** для обычных анализов (дешево, быстро)
2. **GPT-4** для сложных анализов (дороже, лучшее качество)
3. Мониторить расходы через https://platform.openai.com/account/usage

### Для максимальной приватности:
1. **Ollama + LLaMA2** (или Mistral)
2. Работает полностью локально
3. Не требует интернета

---

## 💡 Примеры use cases

### Для тестирования (РЕКОМЕНДУЕТСЯ):
```bash
# Используйте mock анализ (по умолчанию)
# Не требует API ключа
# Хорошо для разработки
```

### Для боевой системы:
```bash
# Установить OpenAI
pip install openai

# Получить ключ: https://platform.openai.com/api-keys
# Добавить в .env: OPENAI_API_KEY=sk-...
# Готово!
```

### Для локальной разработки:
```bash
# Установить Ollama
brew install ollama
ollama pull llama2
ollama serve

# Система автоматически использует localhost:11434
```

---

## 🔌 Как добавить новый провайдер

1. Установить пакет:
   ```bash
   pip install new-llm-package
   ```

2. Добавить env переменную в `.env`

3. Обновить `_run_llm_analysis()` в `llm_service.py`:
   ```python
   def _run_llm_analysis(self, ...):
       if os.getenv('NEW_LLM_KEY'):
           return self._analyze_with_new_llm(...)
       # ... другие провайдеры
       return self._analyze_with_mock(...)
   ```

4. Создать метод:
   ```python
   def _analyze_with_new_llm(self, text, ...):
       # Ваша реализация
       pass
   ```

---

## 📞 Часто задаваемые вопросы

**Q: Какой провайдер выбрать?**  
A: Начните с OpenAI GPT-3.5-turbo. Лучшее соотношение цены и качества.

**Q: Будет ли работать без API ключа?**  
A: Да, система использует mock-анализ по умолчанию. Идеально для разработки.

**Q: Сколько будет стоить?**  
A: OpenAI GPT-3.5-turbo стоит ~$0.0005 за анализ (очень дешево).

**Q: Какая модель лучше?**  
A: GPT-4 лучше качеством, но дороже. GPT-3.5-turbo хороший компромисс.

**Q: Можно ли использовать локальную модель?**  
A: Да, Ollama позволяет запустить LLaMA2 полностью локально.

---

## 📚 Полезные ссылки

- 🔑 OpenAI API Keys: https://platform.openai.com/api-keys
- 📖 OpenAI Docs: https://platform.openai.com/docs
- 🤖 Anthropic Claude: https://www.anthropic.com/
- 🦙 Ollama: https://ollama.ai/
- 💬 OpenAI Models: https://platform.openai.com/docs/models

---

**Начните с OpenAI GPT-3.5-turbo - это проще всего! 🚀**
